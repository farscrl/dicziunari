import { TranslateCutPipe } from './translate-cut.pipe';

describe('TranslateCutPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslateCutPipe();
    expect(pipe).toBeTruthy();
  });
});
